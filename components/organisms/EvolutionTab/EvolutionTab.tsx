import styles from "./EvolutionTab.module.scss";

type Props = {
  currentPokemon: string;
};

const EvolutionTab = (props: Props) => {
  return <div>EvolutionTab</div>;
};

export default EvolutionTab;
